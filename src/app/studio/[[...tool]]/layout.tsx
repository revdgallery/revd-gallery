export const metadata = {
  title: 'REVD Gallery CMS',
  description: 'Content Management System for REVD Gallery',
};

export default function StudioLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
